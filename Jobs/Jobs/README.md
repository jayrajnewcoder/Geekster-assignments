# Frameworks and Language Used :

Frameworks : spring boot
Language : Java

# Data Flow : 

Controller : Controller has all the HTTP methods (GET,PUT,POST,DELETE) which performs equivalent operation
service : buisness logic for the controller layer has been written in the service layer
Repository : created interface JobRepository which extends crudRepositry which is used to perform the database operation on the entity

# Database Design :
 created the entity of type Job which has different fields and we are performing the operations on that entity
 
 # Project Summary:
 
 We have created CRUD operation on the entity of type job and also added some validations on the fields of job entity
